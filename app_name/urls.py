from django.conf.urls import patterns, include, url

urlpatterns = patterns('',
    # url(r'^pages/$', PageListView.as_view()),
    # url(r'^pages/(?P<page_slug>[\w-]+)/$', ScreenshotGroupListView.as_view()),
    # url(r'^pages/(?P<slug>[\w-]+)/(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})/$,', \
    #     ScreenshotGroupListView.as_view())
)